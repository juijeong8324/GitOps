apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: go-network-policy
spec:
  podSelector:
    matchLabels:
      app: go
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: 8080
  egress:
    - {}
